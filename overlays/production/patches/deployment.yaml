apiVersion: apps/v1
kind: Deployment
metadata:
  name: super-proxy
spec:
  replicas: 2
  template:
    spec:
      serviceAccountName: super-proxy-operator
      containers:
        - name: super-proxy
          image: super-proxy-app-image
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          envFrom:
            - configMapRef:
                name: super-proxy-cm